#!/bin/sh

echo "$1: This script is disabled until further notice. Due to massive errors all media batch scripts are disabled"
notify-send "$1 is disabled" "due to massive errors this script disabled until further notice"
exit 1
